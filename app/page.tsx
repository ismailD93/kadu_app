import LandingPage from '../components/LandingPage';

const Page = async () => {
  return (
    <>
      <LandingPage />
    </>
  );
};

const fetchData = async () => {};

export const revalidate = 0;

export default Page;
